<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookbuddy">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Hardware Features (Optional) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookBuddy">
        <activity
            android:name=".activities.MemberDashboardActivity"
            android:exported="false" />
        <!-- Splash Activity (LAUNCHER) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.BookBuddy.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- ALL ACTIVITIES MUST BE DECLARED HERE -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />
        <activity
            android:name=".activities.BookDetailActivity"
            android:exported="false" /> <!-- 👈 THIS WAS MISSING -->
        <activity
            android:name=".activities.AddBookActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MyBooksActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AISearchActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LibrarianDashboardActivity"
            android:exported="false" />
    </application>

</manifest>